---
mailaliases: {} # Meant to be overridden by hieradata/data/$host.yaml
smtpauthusers: {} # Meant to be overridden by hieradata/data/$host.yaml

packages:
  - moreutils # Used by mailman::configentry
  - bash-completion
  - vim-nox
  - strace
  - etckeeper
  - mutt
  - uptimed
  - molly-guard
  - mc
  - screen

extra_packages: [] # Meant to be overridden by hieradata/data/$host.yaml
